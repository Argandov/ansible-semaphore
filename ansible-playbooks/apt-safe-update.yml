---
- name: Safe system updates
  hosts: all
  become: yes

  tasks:

    - name: Update package lists
      apt:
        update_cache: yes
        cache_valid_time: 3600

    - name: Perform safe upgrades (no removals, no breakage)
      apt:
        upgrade: safe
